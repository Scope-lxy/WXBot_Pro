## Windows 中文乱码处理

- 这台机器主要在 Windows 上开发。PowerShell 显示中文源码时，可能会把正常中文显示成 `鑾峰彇` 这类乱码。
- 看到乱码时，先当成“终端显示问题”，不要马上判断文件坏了。
- 读中文文件时，优先按 UTF-8 读取的方式，比如 `Get-Content -Encoding UTF8`、`py -X utf8`，或者支持 UTF-8 的编辑器/API。
- 不要为了“修乱码”直接重写中文内容。只有确认文件字节和原文意思都没问题后，才可以改中文字符串。

## 微信实机测试编码

- 做 wxautox4 / 微信窗口实机测试时，必须先确保 Python 进程使用 UTF-8，否则传给微信窗口的中文昵称可能变成 `??????`，导致找不到真实窗口，测试结果不可信。
- 推荐用项目虚拟环境并显式设置 UTF-8：
  - PowerShell：`$env:PYTHONUTF8='1'; .\venv\Scripts\python.exe ...`
  - 或：`.\venv\Scripts\python.exe -X utf8 ...`
- 临时用 `python -c` 或管道执行脚本时，也要设置 `PYTHONUTF8=1`，并确认脚本能正常打印中文目标名后再判断测试结果。
- 实机测试脚本如果放在 `tools/` 下运行，要确保项目根目录在 `sys.path`，避免 `from feature import ...` 这类项目内导入失败。
- 测试好友可以用：阿英2、阿英3、阿英4、炳3、炳4

## 和用户沟通

- 用户是产品经理背景的 vibe coder，懂产品和一些电脑基础，但通常不看代码。
- 默认用中文沟通。先讲用户能感知到的行为变化、风险和结果，再讲必要的技术细节。
- 不要把“让用户自己看代码”当成验收方式。能跑检查就主动跑，然后用简单中文说明结果。

## 验证和交付

- 完成前，能跑测试或检查就跑。跑不了就直接说明原因，并说明还有什么风险。
- 回复要简洁，用中文说清楚：做了什么、会看到什么变化、怎么验证的、还有没有风险。
- 不要要求用户通过看 diff 或源码来判断结果。把代码变化翻译成产品效果和注意事项。
- 这是个人项目，本地数据可以清洗，不要做没意义的旧版本、旧配置、旧机制兼容，保持代码洁癖。
